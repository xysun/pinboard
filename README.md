This is my local 'pinterest' collection.

It works through Python script and sqlite database.

To add a record, run `python3 shell.py add`. 

Run `sqlite3 bookmarks.db` to enter database interactive mode.
